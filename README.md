# Overview
This is a digital prototype in Vue, designed to explore a problem space that uses both digital and non-digital mediums. 

# Project-Specific Documentation
## Deploy
To deploy to GH Pages: run `npm run build` on the master branch to generate a new build, then run `npm run deploy` to deploy the build to the gh-pages branch.
